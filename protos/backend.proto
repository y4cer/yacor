// This file describes the protobuf for attack service and backend interaction

syntax = "proto3";

package backend;

import "message_definitions.proto";

service AttacksManager {
  rpc subscribe(messages.SubscribeMessage) returns (messages.EmptyMessage) {};
}

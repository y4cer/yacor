version: "3.2"
services:

  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
    command: python backend.py
    restart: unless-stopped
    ports:
      - 50051:50051

  ecdsa_nonce_attack:
    build:
      context: .
      dockerfile: attacks/Dockerfile
    command: python attacks/interact_module.py
    restart: unless-stopped
    depends_on:
      - backend
